<%= form_for(@event) do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="input-field col s6">
      <%= f.label :name, 'Name' %><br>
      <%= f.text_field :name, :class => "validate" %>
    </div>
    <div class="input-field col s6">
      <%= f.label :duration, 'Zeitraum' %><br>
      <%= f.text_field :duration, :class => "validate" %>
    </div>
  </div>

  <div class="row">
    <div class="input-field col s6">
      <%= f.label :description, 'Beschreibung' %><br>
      <%= f.text_area :description, :class => "materialize-textarea" %>
    </div>
    <div class="input-field col s6">
      <%= f.label :start, 'Tag' %><br>
      <%= f.date_field :start, class: 'datepicker' %>
    </div>
  </div>

  <div class="row">
    <div class="actions col s12">
      <button class="btn waves-effect waves-light" type="submit" name="action">Absenden</button>
      <%= link_to 'Zurück', events_path, :class => "waves-effect waves-light btn" %>
    </div>
  </div>
<% end %>
